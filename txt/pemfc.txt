基于机器学习的质子交换膜燃料电池性能寿命预测
1.模型选择：gru：【项目背景】
我需要开发一个基于GRU（门控循环单元）的机器学习模型，用于质子交换膜燃料电池（PEMFC）的性能寿命预测，核心聚焦模型的训练（train）环节。

【用户背景】
我是编程新手，完全不理解GRU模型中各类参数的定义、作用和调优逻辑，需要代码具备极强的可读性和可修改性。

【核心要求】
1. 模型框架：优先使用Python+TensorFlow/Keras（新手友好），核心模型为GRU，聚焦训练流程（数据预处理→模型构建→训练→日志输出）；
2. 参数设计：将GRU所有关键参数（units、dropout、recurrent_dropout等）和训练参数（batch_size、epochs、learning_rate等）集中管理（如参数字典），每个参数标注【作用】【默认值】【修改建议】；
3. 日志系统：使用Python logging模块，输出训练全流程日志（参数配置、数据维度、每轮训练损失/精度、训练耗时、异常信息等），日志格式清晰，方便我复制日志给AI工具辅助修改参数；
4. 注释规范：代码中每一行核心逻辑（尤其是GRU相关）都有详细注释，新手能看懂“为什么这么写”；
5. 数据适配：模拟PEMFC时序数据集（包含电压、电流、温度、湿度、运行时间、寿命剩余等特征），无需真实数据，但要贴合PEMFC寿命预测的时序特性；
6. 输出附加：代码末尾单独列出“GRU参数解释表”和“新手参数修改指南”，说明不同参数修改对模型的影响（如过拟合、训练速度、预测精度）。

【输出格式】
- 完整可运行的Python代码（直接复制即可执行）；
- 代码内包含logging日志配置+训练全流程日志输出；
- 代码末尾附“GRU参数解释”和“新手修改指南”。



2.参数选择：为了降低模型复杂性并提高模型性能，根据 CatBoost 模型评估
结果和 PEMFC退化机制分析选择监测参数：

“请帮我生成一段Python代码，实现**PEMFC燃料电池系统监测参数的重要性分析**，代码需覆盖以下流程（对应我的研究场景）：

1. **数据预处理环节**：
   - 预留数据加载接口（我后续会补充具体数据结构，你可以用示例DataFrame占位）；
   - 对原始数据执行归一化处理；
   - 定义标签与输入特征：以“堆电压”作为模型标签（目标变量），其余监测参数作为输入特征。

2. **参数重要性评估环节**：
   - 使用CatBoost模型训练（需包含模型初始化、训练的代码，无需过度调参，保持基础配置即可）；
   - 从训练后的CatBoost模型中提取**各输入特征的重要性得分**。

3. **结果可视化与排序环节**：
   - 对提取的参数重要性得分按**降序排序**；
   - 绘制与“图3.9”样式一致的柱状图：横轴为监测参数名称（如空气出口流量、冷却水流量等），纵轴为重要性得分，柱状图按重要性从高到低排列。

4. 代码要求：
   - 每一步添加清晰注释，说明对应功能；
   - 可视化部分需保证参数名称在横轴完整显示（可调整布局避免重叠）；
   - 代码模块化，方便我替换自己的实际数据集。

补充背景：我的监测参数包含（但不限于）空气出口流量、冷却水流量、氢气入口温度、电流、电流密度、氢气压力等，代码需兼容这类参数的输入。”
缺失值处理：补充 “线性插值 + 3σ 原则剔除异常值” 的代码逻辑（先插值填充缺失值，再剔除超出均值 ±3 倍标准差的离群点）；
训练 / 验证集划分：按时间顺序拆分（如前 70% 训练，后 30% 验证），禁止 shuffle，并在代码中明确划分逻辑。可视化样式：指定字体（如 Times New Roman）、字号（横轴 / 纵轴标签 12 号，标题 14 号）、柱状图颜色（按重要性梯度着色，如从深红到浅红）；
图片保存：指定保存格式（PNG+PDF）、分辨率（300dpi）、图片尺寸（如 10×6 英寸）。




3.所提模型的预测性能采用四种常用指标来评估，分别为：（a）MAE：平均绝对误差。（b）RMSE：均方根误差。（c）MAPE：平均绝对百分比误差。（d）R2：决定系数。
### 模型训练阶段指标分析与表格生成Prompt

#### 场景与目标
当前处于模型训练阶段，需配合现有训练代码，自动计算模型在（训练集/验证集/测试集）上的4项预测性能指标，并将不同实验条件（如不同学习率）下的指标整理为markdown表格，直观体现模型准确性。

#### 输入数据格式
我会在训练代码中提供以下输入（需与你的函数适配）：
- `y_true`：真实标签数组（如SOH值，类型为numpy数组/torch张量）；
- `y_pred`：模型预测数组（与`y_true`维度一致，类型为numpy数组/torch张量）；
- `condition`：当前实验条件标识（如学习率、窗口大小，类型为字符串/数值）。


#### 功能要求
请实现以下两个核心函数，与训练代码配合运行：
1. **指标计算函数**：
   - 函数名：`calculate_metrics(y_true, y_pred)`；
   - 输入：真实标签`y_true`、预测值`y_pred`；
   - 输出：包含4项指标的字典，键为`["MAE", "RMSE", "MAPE", "R²"]`，值为对应指标的浮点数值（保留4位小数）；
   - 指标计算逻辑：
     - MAE：$\text{MAE} = \frac{1}{n}\sum_{i=1}^n |y_{\text{true},i} - y_{\text{pred},i}|$；
     - RMSE：$\text{RMSE} = \sqrt{\frac{1}{n}\sum_{i=1}^n (y_{\text{true},i} - y_{\text{pred},i})^2}$；
     - MAPE：$\text{MAPE} = \frac{1}{n}\sum_{i=1}^n \left|\frac{y_{\text{true},i} - y_{\text{pred},i}}{y_{\text{true},i}}\right|$（若`y_true`为0需做防除零处理）；
     - R²：$\text{R}^2 = 1 - \frac{\sum_{i=1}^n (y_{\text{true},i} - y_{\text{pred},i})^2}{\sum_{i=1}^n (y_{\text{true},i} - \bar{y}_{\text{true}})^2}$（$\bar{y}_{\text{true}}$为真实标签均值）。

2. **表格生成函数**：
   - 函数名：`generate_metrics_table(metrics_records)`；
   - 输入：`metrics_records`为列表，每个元素是字典（包含`"condition"`键和指标字典），示例：`[{"condition": "20%", "MAE": 0.0027, "RMSE": 0.0039, "MAPE": 0.0008, "R²": 0.9900}, ...]`；
   - 输出：markdown格式的指标表格（与我提供的示例格式完全一致，包含表头“学习率（或实验条件）、MAE、RMSE、MAPE、R²”）；
   - 额外功能：自动将表格打印到控制台，并支持返回表格字符串用于保存到文件。


#### 输出要求
- 函数需兼容numpy和torch张量（若输入为torch张量，需先转为numpy数组计算）；
- 指标数值保留4位小数，与示例格式一致；
- 表格需与以下示例的样式、对齐方式完全匹配：

```markdown
| 学习率 | MAE    | RMSE   | MAPE   | R²     |
|--------|--------|--------|--------|--------|
| 20%    | 0.0027 | 0.0039 | 0.0008 | 0.9900 |
| 40%    | 0.0024 | 0.0036 | 0.0007 | 0.9910 |
```

- 需附带调用示例：说明如何在训练循环中调用`calculate_metrics`收集指标，再用`generate_metrics_table`生成表格。
表格生成函数支持多列实验条件（如表头为 “工况、窗口大小、学习率、MAE、RMSE、MAPE、R²”）；
支持按 “工况 / 窗口大小” 分组汇总指标（如计算同一工况下不同学习率的指标均值）。要求计算多次实验（如 5 次）的指标 “均值 ± 标准差”，并在表格中展示；




4.小波阈值去噪（WTD），对原始数据进行“平滑”去噪：

请基于“小波阈值去噪（WTD）”方法，编写燃料电池数据预处理的去噪代码部分，需严格遵循以下流程、参数与公式：

### 1. 数据定义
将待处理的燃料电池原始时间序列数据定义为：
$$f(t) = \{x_1, x_2, ..., x_{t-1}, x_t\} \quad (i=t, t为时间时刻)$$


### 2. 小波分解步骤
- 对原始时间序列$f(t)$执行**6层小波分解**，得到：
  - 近似系数：$cA_6$（保存信号核心有效特征）；
  - 6层细节系数：$cD_1, cD_2, ..., cD_6$（包含噪声与尖峰）。
- 分解后的小波系数集合表示为：
  $$W_f(a,b) = cA_6 + \sum_{i=1}^6 cD_i$$
  （其中$a$为缩放因子，$b$为时间轴位移因子）


### 3. 阈值计算步骤
1. 对小波系数集合$W_f(a,b)$的所有元素取绝对值，得到$|W_f(a,b)|$；
2. 将$|W_f(a,b)|$按**从小到大排序**，得到排序后的序列；
3. 对排序后的序列中每个元素取平方，得到序列$F(k)$：
   $$F(k) = \left( sort\left(|W_f(a,b)|\right) \right)^2 \quad (k=1,2,...,N; N为原始数据样本数量)$$
4. 计算对应阈值$\lambda_k$：
   $$\lambda_k = \sqrt{F(k)}$$


### 4. 软阈值处理步骤
仅对**细节系数$cD_1 \sim cD_6$（噪声部分）**执行软阈值处理，近似系数$cA_6$保持不变。处理规则为：
$$W_\lambda = \begin{cases}
sign(w) \times (|w| - \lambda_k), & |w| \geq \lambda_k \\
0, & |w| < \lambda_k
\end{cases}$$
（其中$w$为细节系数元素，$sign(w)$为符号函数：$w>0$时取1，$w<0$时取-1）


### 5. 数据重构步骤
使用“处理后的细节系数”+“原始近似系数$cA_6$”执行**小波逆分解（重构）**，输出去噪后的燃料电池数据。


请基于上述流程编写数据预处理的去噪代码，确保每一步匹配对应的公式与参数。
补充需求：仅实现去噪流程，未验证去噪效果，无法判断去噪是否合理。
补充内容：
新增去噪效果评估指标：信噪比（SNR）、均方根误差（RMSE）、平滑度（如去噪后数据的一阶差分方差）；
对比去噪前后的电压曲线可视化（新增子图展示去噪效果）。
小波基函数：指定燃料电池数据常用的sym8（需说明选择依据）；
阈值 k 值：明确排序后的 F (k) 取 “90 分位数”（或中位数）作为阈值（燃料电池噪声多为高频小幅度，90 分位数可保留有效信号）；





5.对所有训练完成的模型进行可视化展示
### 可视化模块代码撰写Prompt

#### 背景与目标
我需要撰写项目中“燃料电池堆电压预测结果可视化”的Python代码（基于matplotlib库），核心目标是对所有训练完成的模型输出结果进行可视化展示，生成与指定示例图（图3.10）样式一致的曲线对比图。代码需具备可运行性，能直接与主代码的输出数据衔接，无需额外修改核心逻辑即可替换真实数据使用。

#### 可视化核心样式与元素要求
1. **曲线定义**：
   - 蓝色实线：代表燃料电池堆电压的真实值；
   - 绿色实线：代表模型在训练数据集上的电压预测结果（仅覆盖训练时段）；
   - 红色实线：代表模型在测试数据集上的电压预测结果（仅覆盖测试时段）。
2. **坐标轴配置**：
   - 横轴（X轴）：标签为“时间/h”，范围自动适配输入的时间序列数据；
   - 纵轴（Y轴）：标签为“电压/V”，范围需自动覆盖电压真实值与预测值的全部区间。
3. **辅助视觉元素**：
   - 垂直虚线：标记训练阶段与测试阶段的分隔时间点，标注“训练阶段”“预测阶段”文字；
   - 水平虚线：标记电压失效阈值，标注“失效阈值 EOL=XX h”（XX为失效时间，从主代码传入）；
   - 箭头标注：在测试阶段区域添加“剩余使用寿命”文字及箭头指向对应区域；
   - 图例：位于图表右上角，清晰标注“原始数据（真实值）”“训练集预测结果”“测试集预测结果”。
4. **标题与保存**：
   - 图表标题：动态包含训练阶段的时长（如“训练长度为XXX小时预测寿命预测结果”，XXX为训练结束时间，从主代码传入）；
   - 图片保存：支持将可视化结果保存为高清图片（分辨率≥300dpi），保存路径可配置。

#### 代码技术要求
1. 代码兼容Python 3.8及以上版本，仅依赖matplotlib和numpy库；
2. 使用通用化变量名（如时间序列、真实电压、训练集预测电压、测试集预测电压等），可直接对接主代码输出的变量，无需修改变量名即可替换数据；
3. 自动处理训练/测试集数据的时段匹配：通过掩码逻辑仅在对应时段绘制训练/测试预测曲线，避免数据维度不匹配问题；
4. 代码结构清晰，包含完整的函数定义，函数内添加详细注释说明各元素的绘制逻辑；
5. 提供简单的调用示例（仅保留占位符，无需真实数据），示例中明确标注“替换为主代码输出的真实数据即可运行”。
置信区间计算：基于高斯 NLL 输出的 “均值 + 标准差”，95% 置信区间为[mean - 1.96*std, mean + 1.96*std]；
代码中明确从模型输出提取均值和标准差的逻辑（如模型输出为[pred_mean, pred_std]）。补充内容：
新增 SOH 曲线子图（横轴时间，纵轴 SOH，标注 0.96 故障阈值线）；
标注真实 RUL 和预测 RUL 的时间点（如垂直红线标注预测 RUL，蓝线标注真实 RUL）；
计算 RUL 预测误差（绝对误差、相对误差）并标注在图中。



6.重要参数选择：将初始电压的96%设定为质子交换膜燃料电池在稳态和动态条件下的故障阈值
我们定义SOH为：SOH_t = V_t / V_initial，其中V_initial是初始时刻的电压（是前100个点的平均）。这样SOH从1开始，随着退化逐渐降低。RUL定义为到达某个阈值的时间，电压损失百分比等于功率损失百分比。因此，我们可以设定电压阈值为：阈值电压 = V_initial * (1 - 损失百分比)
补充内容：
V_initial 计算：先对前 100 个电压点做 SG 滤波 / 小波去噪，再取均值；
补充 V_initial 的稳定性验证（如计算前 100 点的变异系数 CV<1% 则认为稳定，否则扩大至前 200 点）。
RUL 插值逻辑：当 SOH 在 t1 和 t2 之间跨越阈值（SOH (t1)>0.96，SOH (t2)<0.96），用线性插值计算精确 RUL：\(RUL = t1 + (0.96 - SOH(t1)) * (t2 - t1) / (SOH(t2) - SOH(t1))\)明确 RUL 的评估指标（如 RUL 绝对误差 (MAE_RUL)、RUL 相对误差 (MAPE_RUL)、评分函数（PHM 挑战赛标准））。

全流程添加异常捕获逻辑（如数据加载失败、模型训练中断、可视化保存路径不存在等），避免代码崩溃。补充：要求各模块添加单元测试用例（如数据预处理模块测试 SG 滤波效果、指标函数测试计算正确性）；
补充：添加训练日志功能（记录每轮迭代的损失、指标、超参数，保存为 log 文件）。
7.总结一下：我目前想在前端达成的目的如下：
1，总体上来讲这是一个pemfc寿命预测软件，按文件来分有以下几个流程：
1，重要性分析，pemfc_catboost_analysis.py这个文件用于我对于数据中其他列关于时间对电压变化的重要性排序，这点在前端已经有界面显示
产出文件位置描述：名为 catboost_results 的文件夹，专门用于存储CatBoost 机器学习模型的分析结果。文件夹内的文件均带有时间戳（20251206_1953xx，即 2025 年 12 月 6 日 19 点 53 分左右），具体文件及含义是：
feature_importance_results_20251206_19532. CSV；
cumulative_importance_20251206_195332.png：累积特征重要性的可视化图片；
feature_importance_20251206_195332.png：单个特征重要性的可视化图片；
voltage_timeseries_20251206_195332.png：电压时间序列的可视化图表（推测是模型关联的监测数据时序图）；
analysis_report_20251206_19532.txt：本次 CatBoost 分析对应的文本格式报告文件。
我希望前端可以集成操作为点击分析，即运行pemfc_catboost_analysis.py文件并产出csv，点击生成图表即可使用对应路径的csv进行图表美化后的展示

2.源数据展示，我希望在重要性分析后展示这几个数据的原始数据，1默认为	时间、堆电压、电流、电流密度、氢气入口温度、冷却水流量和空气出口流量  这几个参数使用不同颜色显示在一个图表中，2为各个数据分别以时间为轴，跟电压一同展示在一个表中。1 2 有按钮切换。注意：原始数据为分开的几个文件，示例data文件夹下，直接存放了FC1 燃料电池的老化数据文件：FC1_Ageing_part1.csv、FC1_Ageing_part2.csv、FC1_Ageing_part3.csv（CSV 是表格类数据格式，“part” 表示数据因体积较大被拆分为多个部分）；文件夹datatest，这里存放的是FC2 燃料电池的老化测试数据文件：FC2_Ageing_part1.csv、FC2_Ageing_part2.csv（“datatest” 对应测试用途的数据集）。这两个文件夹内的文件为源文件，绝对不会有任何变动，可以直接写成固定的相对格式，直接读取，但流程应该是在重要性排序之后，但实际上我们默认选择前5项作为展示
请注意横轴为时间纵轴为其他数据，但请你注意归一化或者说图表的美观程度
我希望在显示时候将同一文件夹下的文件结合为一个大的csv文件，储存在visualization/truedata中，显示为带有时间戳（20251206_1953xx，即 2025 年 12 月 6 日 19 点 53 分左右）的格式
注意：此部分没有写入前端中，可能需要你另外生成文件在gui文件夹中，但注意调用，使我集成的文件PEMFC_Integrated_Tool.py可以直接调用
请注意：fc1与fc2都应该可以展现才对，按照上方的要求为fc2设置一样的按钮，总体上设置1 与2切换的按钮就可以，注意时间轴的适应性，因为fc2只有两部分

3.数据处理阶段。目前我已经有文件data_processing.py 它也是直接读取源文件csv如2点，在经过处理之后生成csv和npz文件
输出格式如：顶层processed_results文件夹：专门存储经清洗、加工后的燃料电池数据结果；
子文件夹按燃料电池分类：
FC1子文件夹：存放1 号燃料电池（FC1）的处理后数据，包含两个文件：
FC1_processed_20251206_215017.csv：FC1 处理后的表格型数据文件（CSV 格式，便于查看和常规分析）；
FC1_processed_20251206_215017.npz：FC1 处理后的 NumPy 压缩数据文件（NPZ 格式，通常用于存储数组类数据，适配机器学习等程序读取）；
FC2子文件夹：存放2 号燃料电池（FC2）的处理后数据，包含：
FC2_processed_20251206_215047.csv：FC2 处理后的表格型数据文件；
FC2_processed_20251206_215047.npz：FC2 处理后的 NumPy 压缩数据文件。
文件名中的processed表示 “已处理”，20251206_xxxx是数据处理完成的时间戳（2025 年 12 月 6 日晚 21 点左右）。
我目前需要你在前端中也集成一个这个操作，即我点击处理数据（滤波对比按钮）即为我运行此文件data_processing.py ，并在文件夹里捕获fc1和fc2的csv文件，1在界面上画出美化后的电压与时间的图 2另外几个参数关于时间的变化图 12分别命名并产出切换按钮，另外在总体上还需要能切换fc1 和fc2 的显示

4.最主要的的核心：train.py此文件是我代码的核心，我在此代码中运用重要性排序中的几个因素，使之成为电压的目标特征，在时序下训练模型，最后在fc1的部分中验证模型准确性，最后得出fc2的目标结论：其电压到达96阈值的时间即为fc2寿命（此部分可做待定，因为train部分的代码还没做好）
对于此部分我在前端上的要求：由于滑动窗口大小和尖峰阈值都已经设定好，我希望在前端显示小波去噪和滑动窗口的重要参数，设定按钮名称为为启动寿命预测，此直接启动train。py读取写好路径的npz文件，后进度条读取日志并显示日志中关键节点，当train过程结束后，显示窗口提示模型训练完成
1在train 的输出路径中寻找fc1的prediction的csv文件和读取时npz路径下的csv文件，点击按钮（你命名一个合适的名字）,即可显示fc1的真实数据和预测数据的差别，不同颜色显示
2 在train 的输出路径中寻找fc2的prediction的csv文件和读取时npz路径下的csv文件点击按钮（你命名一个合适的名字）我希望可以显示fc2在模型下的预测，时间轴上与fc1相同？（待定）
但注意，fc1有完整时间轴的全部数据，但fc2只有两部分，fc2可能无法对比显示，这一部分待定

5.总体上，我希望实现页面的美观和流畅。另外我还希望日志功能可以做到捕获重要的运行信息作为显示，报错也有相应的报错信息。进度条需要保证流畅和美观。














